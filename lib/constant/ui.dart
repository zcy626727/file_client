import 'package:flutter/cupertino.dart';

const double dialogTitleFontSize = 20;
const containerBorder = BorderRadius.all(Radius.circular(10));
const dialogContentPadding = EdgeInsets.symmetric(horizontal: 14, vertical: 16);
